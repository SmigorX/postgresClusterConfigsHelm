Project that condenses all manifests needed for CNFC Postgres Operator cluster deployment into single helmchart.
This chart does not include the operator itself which should be installed before deploying this chart.
